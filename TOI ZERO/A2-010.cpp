#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, q;
    cin>> n>> q;
    vector<vector<int>> depth(100);
    int de = 0;
    for (int i = 0; i<n; i++) {
        int d, l;
        de+=d;
        depth[de] = l 
    }
    return 0;
}