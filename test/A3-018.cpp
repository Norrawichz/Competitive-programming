#include <iostream>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int l, n;
    cin>> l>> n;
    int current = 0;
    for (int i = 1; i<=l; i++) {
        int a = i*i
    }
    return 0;
}