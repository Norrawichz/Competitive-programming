#include <bits/stdc++.h>
using namespace std;

int recur(int cur, vector<int> &a, vector<int> &c) {
    if (cur < 0) return 0;

    int mn=INT_MAX;

}

int main() {
    cin.tie(0)->sync_with_stdio(0);
    int t;
    cin>> t;

    while (t--) {
        int n;
        cin>> n;

        vector<int> a(n), c(n);
        for (int i=0; i<n; i++) cin>> a[i];
        for (int i=0; i<n; i++) cin>> c[i];

        

    }
}