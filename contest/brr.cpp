#include<bits/stdc++.h>
using namespace std;

struct dvc{
    int d, v, c;
};

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int n, k, t;
    cin>> n>> k>> t;
    dvc num[n];
    set<int> vsum = {};
    vector<int> sorted;
    for (int i = 0; i<n; i++) {
        cin>> num[i].day>> num[i].value>> num[i].code;
    }
    for (int i = 0; i<n; i++) {
        for (int j = 0; j<n; j++) {
            if 
        }
    }
    
    return 0;
}