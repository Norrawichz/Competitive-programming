#include <bits/stdc++.h>
using namespace std;

int main() { 
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int score[4];
    string name[4];
    for (int i = 0; i<4; i++) {
        cin>> name[i];
    }
    int board[4][4];
    for (int i = 0; i<4; i++) {
        for (int j = 0; j<4; j++) {
            cin>> board[i][j];
        }
    }
}