#include <stdio.h>
#include <string.h>

int main() {
    char s[100];
    int n;
    scanf("%s %d", &s, n);

    int bs=(strlen(s)-1)*(n-1) + n;
    char b[n][b];

    int si=0, sj=0;
    for (int i=0; i<bs; i++) {
            
    }
}