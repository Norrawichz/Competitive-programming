#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);
    int n, l, r;
    vector<int> dp(n+1, 0);
    dp[0] = 1;

    for (int i=1; i<=n; i++) {
        if (i>)
    }
}